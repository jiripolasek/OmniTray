<Page
    x:Class="OmniTray.Views.NoteLibraryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:OmniTray.Controls"
    xmlns:views="using:OmniTray.Views">
    <Grid x:Name="RootGrid" Padding="20" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="Notes" Style="{ThemeResource TitleTextBlockStyle}" />
        <Grid Grid.Row="1" ColumnSpacing="12">
            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
            <ComboBox x:Name="ModeBox" SelectedIndex="0" SelectionChanged="OnModeChanged" AutomationProperties.Name="Notes view">
                <ComboBoxItem Content="Recently edited" />
                <ComboBoxItem Content="Recently deleted" />
            </ComboBox>
            <TextBox x:Name="SearchBox" Grid.Column="1" PlaceholderText="Search notes and their locations"
                AutomationProperties.Name="Search notes" TextChanged="OnSearchChanged" />
        </Grid>
        <CommandBar x:Name="Commands" Grid.Row="2" Background="Transparent" DefaultLabelPosition="Right">
            <AppBarButton Label="New note" Click="OnNewClick">
                <AppBarButton.Icon><FontIcon Glyph="&#xE70B;" /></AppBarButton.Icon>
            </AppBarButton>
            <AppBarButton Icon="Paste" Label="Clipboard → note" Click="OnClipboardClick" />
            <AppBarButton x:Name="OpenButton" Label="Open" Click="OnOpenClick" IsEnabled="False">
                <AppBarButton.Icon><FontIcon Glyph="&#xE70B;" /></AppBarButton.Icon>
            </AppBarButton>
            <CommandBar.SecondaryCommands>
                <AppBarButton x:Name="DeleteButton" Icon="Delete" Label="Delete permanently" Click="OnPurgeClick" Visibility="Collapsed" />
            </CommandBar.SecondaryCommands>
        </CommandBar>
        <ListView x:Name="NotesList" Grid.Row="3" ItemsSource="{x:Bind Entries}" SelectionMode="Single"
            DoubleTapped="OnNotesDoubleTapped" KeyDown="OnNotesKeyDown" SelectionChanged="OnSelectionChanged"
            AutomationProperties.Name="Notes">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="views:NoteLibraryEntry">
                    <Grid Padding="4,8" ColumnSpacing="12">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="40" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                        <controls:NotePreview Note="{x:Bind Note}" Compact="True" Width="40" Height="40" VerticalAlignment="Top" />
                        <StackPanel Grid.Column="1" Spacing="3">
                            <TextBlock Text="{x:Bind Note.DisplayName}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{x:Bind Preview}" MaxLines="2" TextWrapping="Wrap" TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{x:Bind Location}" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{x:Bind TimeText}" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <TextBlock x:Name="SummaryText" Grid.Row="4" TextWrapping="Wrap" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        <InfoBar x:Name="ErrorBar" Grid.Row="5" IsOpen="False" IsClosable="True" Severity="Error" />
    </Grid>
</Page>
